<!DOCTYPE html>
<html lang="en">
    <header>
        <%- include ('./partials/header.ejs') %>
    </header>
    <body>
        <button style="margin: 10px;" ><a href="/<%= furryFriend.id %>/edit" class="edit" >Edit</a></button>
        <h1><%= furryFriend.name %></h1>
        <h2><%= furryFriend.age %></h2>
        <img class="showImage" src="<%= furryFriend.img %>">
        <h3><%= furryFriend.color %></h3>
        <h3><%= furryFriend.gender %></h3>
        Good with Kids: <input type="checkbox" id="kids" name="kids" disabled="disabled"
            <% if(furryFriend.kids === true) { %> 
                style = "outline: 2px solid gold"
                checked
            <% } %>
            >
        Good with Dogs: <input type="checkbox" id="dogs" name="dogs" disabled="disabled"
            <% if(furryFriend.dogs === true) { %> 
                style = "outline: 2px solid gold"
                checked
            <% } %>
            >
        Good with Cats: <input type="checkbox" id="cats" name="cats" disabled="disabled"
            <% if(furryFriend.cats === true) { %> 
                style = "outline: 2px solid gold"
                checked
            <% } %>
            >
        <form class="trash" action="/<%= furryFriend.id %>?_method=DELETE" method="POST" style="padding: 30px;" >
            <input type="submit" value="Delete" class="delete" style="background-color: red;">
        </form>
    </body>
    <footer>
        <%- include ('./partials/footer.ejs') %>
    </footer>
</html>